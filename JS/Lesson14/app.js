// 27 january 2024

window.addEventListener("DOMContentLoaded", function () 
{
    "use strict";

    const qs = (tag) => this.document.querySelector(tag);
    const gebi = (tag) => this.document.getElementById(tag);

    // setTimeout
    // 1s = 1000ms
    this.setTimeout(function () {}, 1000);

    // setInterval
    let timer = this.setInterval(function () {}, 1000);

    // Stops the interval
    this.clearInterval(timer);

    // Create sound 
    let audio = new Audio(link);

    // Play sound
    audio.play();

    // Time
    let time = new Date();
    let hours = time.getHours();
    let minutes = time.getMinutes();
    let seconds = time.getSeconds();
    let milliseconds = time.getMilliseconds();
    let year = time.getFullYear();
    let month = time.getMonth();
    let day = time.getDate();
    let dayOfWeek = time.getDay();

    let oldDate = new Date(2021, 0, 1);
    // (year, month, day, hours, minutes, seconds, milliseconds)
});



