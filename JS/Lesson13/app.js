// 25 january 2024

// filter((item) => item.key === value) - returns array of objects that meet condition

// console.table() - shows objects in array in table

// some() - returns true if at least one item in array meets condition
// some(() => item.key === value) boolean

// every() - returns true if all items in array meet condition
// every(() => item.key === value) boolean

// find() - returns first item in array that meets condition
// find(() => item.key === value) object 

// findIndex - returns index of first item in array that meets condition
// findIndex(() => item.key === value) number

// sort((a, b) => a > b ? 1 : -1) 
// sort((a, b) => a.key > b.key ? 1 : -1)
// sort((a, b) => a.key > b.key ? -1 : 1) - reverse

// reduce((a, b) => a + b.key, 0)  for object, 0 is initial value 
// reduce((a, b) => a + b, 0)  for array, 0 is initial value
// reduce() - returns single value which is sum. number

// accumulator - total value
// currentValue - current item in array


// tag.innerHTML = ``;  // clear tag


// array.toSorted - clones the array
// array.toReverse - clones the array and reverses it 
